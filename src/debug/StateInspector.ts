/**
 * StateInspector — XState inspector integration.
 *
 * Placeholder stub. Populated in a later sprint when the actor hierarchy
 * is established. Intended to wire up `@xstate/inspect` or the Stately
 * browser extension inspector so state machine transitions are visible in
 * dev tools.
 *
 * Only active when `?debug=true&showStateOverlay=true`.
 */

// eslint-disable-next-line @typescript-eslint/no-extraneous-class -- stub, methods added in later sprint
export class StateInspector {}
